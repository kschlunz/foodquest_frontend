document.addEventListener('DOMContentLoaded', () => {
  Card.showCards()

  // fetch("http://localhost:3000/api/v1/users")
  // .then(res=> res.json())
  // .then(function(json){
  //   console.log(json)
  //   Card.showCards([json])
  // })
})

  // Adapter.getCards().then(function(json) {

     // Card.showCards([json])

   // })
